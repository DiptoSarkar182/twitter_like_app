<!DOCTYPE html>
<html>
  <head>
    <title>TwitterLikeApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= favicon_link_tag "twitter_favcon.png" %>
  </head>

  <body class="bg-gray-100">
  <nav class="bg-white shadow-md">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center justify-between h-16">
        <div class="flex items-center">
          <%= link_to 'Twit Clone', root_path, class: "text-lg font-semibold text-gray-800" %>
        </div>
        <div class="flex items-center">
          <% if user_signed_in? %>
            <div class="mr-4 text-gray-600">
              Hello, <%= current_user.full_name %>
            </div>
            <div class="ml-4 flex items-center md:ml-6">
              <%= link_to "New Post", new_post_path, class: "px-3 py-2 rounded-md text-sm font-medium text-gray-300 bg-gray-900 hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-gray-800 focus:ring-white" %>
              <%= link_to "Edit Account", edit_user_registration_path, class: "px-3 py-2 rounded-md text-sm font-medium text-black-400 hover:bg-gray-700 hover:text-white focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-gray-800 focus:ring-white" %>
              <%= link_to "Profile", users_profile_path(current_user), class: "px-3 py-2 rounded-md text-sm font-medium text-black-400 hover:bg-gray-700 hover:text-white focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-gray-800 focus:ring-white" %>
              <%= button_to "Log Out", destroy_user_session_url, method: "delete", class: "px-3 py-2 rounded-md text-sm font-medium text-gray-300 bg-gray-900 hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-gray-800 focus:ring-white" %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </nav>

  <% if alert || notice %>
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
      <div data-controller="flash" class="rounded-md bg-green-50 p-4">
        <div class="flex">
          <div class="flex-shrink-0">
            <svg class="h-5 w-5 text-green-400" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
            </svg>
          </div>
          <div class="ml-3">
            <% if notice %>
              <p class="text-sm font-medium text-green-800"><%= notice %></p>
            <% end %>
            <% if alert %>
              <p class="text-sm font-medium text-green-800"><%= alert %></p>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  <% end %>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <%= yield %>
  </div>
  </body>
</html>
